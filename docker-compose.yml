version: '3.8'

services:
  php-apache:
    build:
      context: .
    ports:
      - "8080:80" # Map port 8080 on your machine to port 80 in the container
    volumes:
      - .:/var/www/html # Map your project directory to the container
    container_name: php-environment